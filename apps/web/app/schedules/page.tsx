import { Shell } from '../../components/Shell';

export default function SchedulesPage() {
  return (
    <Shell>
      <h1>Schedules</h1>
      <section className="card">
        <p className="muted">Production module screen for schedules. Backend APIs are available under /api/v1.</p>
      </section>
    </Shell>
  );
}
