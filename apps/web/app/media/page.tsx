import { Shell } from '../../components/Shell';

export default function MediaPage() {
  return (
    <Shell>
      <h1>Media</h1>
      <section className="card">
        <p className="muted">Production module screen for media. Backend APIs are available under /api/v1.</p>
      </section>
    </Shell>
  );
}